<?xml version="1.0" encoding="UTF-8" ?>
<constraint-mapping xmlns="http://symfony.com/schema/dic/constraint-mapping"
                    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                    xsi:schemaLocation="http://symfony.com/schema/dic/constraint-mapping
        https://symfony.com/schema/dic/constraint-mapping/constraint-mapping-1.0.xsd">

    <class name="App\Entity\User">

        <property name="username">
            <constraint name="NotBlank"/>
            <constraint name="Length">
                <option name="min">6</option>
                <option name="max">255</option>
                <option name="minMessage">
                    Username must be at least {{ limit }} characters long.
                </option>
                <option name="maxMessage">
                    Username must no exceed {{ limit }} characters.
                </option>
            </constraint>
        </property>

        <property name="plainPassword">
            <constraint name="NotBlank"/>
            <constraint name="Regex">
                <option name="pattern">/(?=.*[a-z])(?=.*[A-Z])(?=.*[0-9]).{8,}/</option>
                <option name="message">Minimum length is 8. The password must also contain one uppercase, one lowercase letter and one digit.</option>
            </constraint>
        </property>

        <property name="retypedPlainPassword">
            <constraint name="NotBlank"/>
            <constraint name="Expression">
                <option name="expression">this.getPlainPassword() === this.getRetypedPlainPassword()</option>
                <option name="message">Passwords do not match.</option>
            </constraint>
        </property>

        <property name="email">
            <constraint name="NotBlank"/>
            <constraint name="Email"/>
            <constraint name="Length">
                <option name="max">255</option>
                <option name="maxMessage">
                    Your email must no exceed {{ limit }} characters.
                </option>
            </constraint>
        </property>

        <property name="fullname">
            <constraint name="NotBlank"/>
            <constraint name="Length">
                <option name="max">255</option>
                <option name="maxMessage">
                    Your name must no exceed {{ limit }} characters.
                </option>
            </constraint>
        </property>

    </class>

</constraint-mapping>